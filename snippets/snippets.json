{
  "gulpfile.js generator ": {
    "prefix": "configs-gulp",
    "body": [
      "//! this files name should be >>>> gulpfile.js <<<<<",
      "//* this files name should be >>>> gulpfile.js <<<<<",
      "",
      "// todo:  install the following packages",
      "// 1. npm init",
      "// 2. npm i -D gulp gulp-sass browser-sync",
      "",
      "const gulp = require(\"gulp\");",
      "const sass = require(\"gulp-sass\");",
      "const browserSync = require(\"browser-sync\").create();",
      "",
      "//* if the scss files are in the \"src\" folder, then uncomment the lines with the symbol next to them \"<<  *number*  >>\"*",
      "",
      "// compile scss into css",
      "",
      "// there is three steps",
      "function style() {",
      "  return (",
      "    gulp",
      "      // 1. where is my scss file",
      "      .src(\"./scss/**/*.scss\") // if the folder is in the root directory",
      "      //*1*   .src(\"./src/scss/**/*.scss\") // incase the folder is in the src folder",
      "      // 2. pass that file through sass compiler",
      "      //   .pipe(sass())",
      "      .pipe(sass().on(\"error\", sass.logError))",
      "      // 3. where do I save the compiled CSS ?",
      "      .pipe(gulp.dest(\"./css\"))",
      "      // 4. stream changes to all browser",
      "      .pipe(browserSync.stream()) // this will stream the css file to all the browsers once the CSS is written",
      "  );",
      "}",
      "",
      "function watch() {",
      "  browserSync.init({",
      "    server: {",
      "      // the base directory",
      "      baseDir: \"./\"",
      "    }",
      "  });",
      "  // watch the scss files for changes and when there is changes",
      "  // then run the style() function",
      "  // *2* //   gulp.watch(\"./src/scss/**/*.scss\", style);",
      "  gulp.watch(\"./scss/**/*.scss\", style); // here it will not refresh the page like it is with the changes in the html and JS files",
      "  // this is useful when you are working on a certain section on the bottom",
      "",
      "  // when you make changes to html .. we want the browser to fully refresh",
      "  gulp.watch(\"./*.html\").on(\"change\", browserSync.reload);",
      "  // watch for the changes for JS",
      "  // *3* //  gulp.watch(\"./src/js/**/*.js\").on(\"change\", browserSync.reload);",
      "  gulp.watch(\"./js/**/*.js\").on(\"change\", browserSync.reload);",
      "}",
      "",
      "exports.style = style;",
      "exports.watch = watch;",
      "",
      "// in the terminal write",
      "// gulp watch  // watch is the name of the function",
      ""
    ],
    "description": "gulpfile.js generator "
  }
}
